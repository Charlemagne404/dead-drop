/** The frontend value paired with the secure v2 Rust protocol contract. */
export const CURRENT_PROTOCOL_VERSION = 2;

/** Keep the UI queue intentionally small while the native service remains single-transfer. */
export const MAX_QUEUED_TRANSFERS = 8;
