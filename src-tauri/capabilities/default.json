{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dead Drop desktop capability set",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
